import styled from "styled-components";

const Input = styled.input`
    padding: 0.7rem;
    background: #fff;
    border: none;
    border-radius: 5px;
    width: 15rem;

    &::placeholder {
      color: #c1c1c1;
    }
`

export default Input;